<h1>Show Project</h1>

<ul>

  <li>
    <strong>Name:</strong>
    <%= @project.name %>
  </li>

</ul>

<span><%= link "Edit", to: Routes.project_path(@conn, :edit, @project) %></span>
<span><%= link "Back", to: Routes.project_path(@conn, :index) %></span>


<h1>Listing Ideas</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>

      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for idea <- @ideas do %>
    <tr>
      <td><%= idea.title %></td>
      <td><%= idea.description %></td>

      <td>
        <span><%= link "Show", to: Routes.project_idea_path(@conn, :show, @project.id, idea) %></span>
        <span><%= link "Edit", to: Routes.project_idea_path(@conn, :edit, @project.id, idea) %></span>
        <span><%= link "Delete", to: Routes.project_idea_path(@conn, :delete, @project.id, idea), method: :delete, data: [confirm: "Are you sure?"] %></span>
      </td>
    </tr>
<% end %>
  </tbody>
</table>

<span><%= link "New Idea", to: Routes.project_idea_path(@conn, :new, @project.id) %></span>
